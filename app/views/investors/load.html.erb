<h3>Load an existing Investments.json file</h3>
<p>Enter the URL of a publicly-available Investments.js file below, and we'll load it for editing.</p>
<%= simple_form_for :investment, :url => '/investors/parse', :html => { :class => 'form-horizontal' } do |investment| %>
  <div class="well">
    <%= investment.input :url, :as => :string, :label => "Investments.js URL", :hint => "Please make sure to enter a complete URL!", :input_html => { :class => "span5" } %>
  </div>
  <%= investment.button :submit, "Fetch my Investments.js file", :class => 'btn btn-primary btn-large' %>
<% end %>